# 教材制作ルール

このプロジェクトで学習教材を作るときの決めごと。W1以降の教材もこの形式で追加する。

## 出力形式と置き場所

- **HTML1枚／単元** で作る。図・インタラクティブ要素をmdより自由に扱えるため
- 置き場所：ユーザーのMacの `~/Desktop/簿記2級教材/<phase>/`
- 共通アセット `<phase>/assets/style.css` と `<phase>/assets/app.js` を全ページで共有する
- 各フェーズに目次ページ `index.html` を置き、単元ページから `← 目次` で戻れるようにする
- 外部CDN・外部フォント・画像URLを参照しない。**オフラインで `file://` から直接開いて動くこと**

## 執筆方針

- 読者は3級合格済み・会計が専門ではない社会人
- 手本は https://studyboki3.com/ 。用語を出す前に「何が困るからこの仕組みがあるのか」を書き、身近な例えを先に置き、「なぜそうなるのか」を必ず言語化する
- つまずきやすい所は先回りして警告する（`.callout--trap`）
- **ドリルで問う判断は、必ず本文に根拠がある状態にする。** 本文に書いていないルールを演習だけで問わない
- 制作過程や検討の経緯は成果物に書かない。最終的な設計だけを提示する
- 絵文字は使わない

## 共通アセットが提供するもの

`assets/style.css`

- ライト／ダーク両対応（CSS変数。`prefers-color-scheme` ＋ `data-theme` トグル）
- `.card` `.callout(--why/--warn/--trap/--ok/--mine)` `.term` `.steps` `details.acc` `.toc` `.grid2/3`
- `.callout--mine` は学習者個人向けの追記専用。「学習者個人に合わせた追記」を読むこと
- 仕訳表 `table.jnl`（借方 `.d` ／貸方 `.c` ／金額 `.amt`）、T字勘定 `.taccounts`
- 表は `<div class="tablewrap">` で包む（横スクロール）。`.grid2/3` の子は `min-width:0` にしてあるので、グリッド内に表を置いてもページ全体が横に広がらない
- 選択肢 `.choice` はCSSカウンタで自動採番される。解説から「選択肢3の…」と番号で参照してよい
- SVG図は `.figure > .svgwrap`。色は必ずCSS変数を使い、生のhexを書かない（ダークモード対応のため）
- チェックリスト `.checklist` ＋ `.progressbar`

`assets/app.js`（外部ライブラリ不使用）

| API | 用途 |
|---|---|
| `BokiJournal.mount(sel, cfg)` | 仕訳入力ドリル。勘定科目プールから検索して選択、金額入力、集合一致で自動採点 |
| `BokiQuiz.mount(sel, cfg)` | 選択式クイズ |
| `BokiNum.mount(sel, cfg)` | 数値回答ドリル（複数欄・許容誤差対応） |
| `BokiFill.mount(sel, cfg)` | 用語穴埋め（許容表記の配列で判定） |

- 仕訳ドリルの解答欄は**常に4行**。行数から正解の科目数が読めると本番の第1問と条件が変わるため。未入力行は採点で無視される
- `cfg.recordAs` と `cfg.qNumbers` は復習ドリルが記録先を元の単元に向けるための口。単元HTMLでは使わない
- `input[type=checkbox][data-key]` は自動で学習記録に保存される
- テーマ切替ボタンは `.theme-btn` を置くだけで動く

### 学習メモ

    <div data-note></div>

その位置にメモ欄が生える。書かれた内容は単元と日時に紐づいて記録される。
各単元に1つ置く。

### 学習記録（著者が書くことはない）

ドリルの正誤・チェックの状態・学習時間は `assets/app.js` が自動で記録する。
単元HTMLに記録用のコードを書く必要はない。

ただし **`mount()` 先の要素には必ず `id` を付ける。** id がないと記録先が
決まらず、その設問は永久に記録されない（ゲート9が検出する）。

記録は `localStorage` の `boki2:progress` に単元キー付きで入るため、
ページをまたいで同じ `id` や `data-key` を使っても混ざらない。

**`id` と設問の並び順は、いったん公開したら変えない。** 記録IDは
`単元キー#マウント先のid/q番号` で、番号は設問の並び順で決まる。途中に
設問を挿すと、それ以降の設問の記録が1つずつずれ、学習者が過去に間違えた
設問と結びつかなくなる。**設問を足すときは末尾に足す。**

ずれても番号としては有効なままなので、画面には何も現れない。復習ドリルは
「間違えた覚えのない設問」を出し、その正解を元の設問IDに積んで一覧から
消してしまう。実際に間違えた設問は復習されないまま消える。ゲート13が
`reference/drill-ids.json` と突き合わせてこれを検出する。

設問の文言を推敲して指紋が変わったときは、`reference/drill-ids.json` を
更新してコミットする。**中身が同じ設問だという判断は人間が下す**（機械には
推敲と差し替えの区別がつかない）。

### 設問バンク（復習ドリル用）

    npm run build:drills     # assets/drills.json を生成する

`review.html`（復習ドリル）は、一度でも間違えた設問を単元をまたいで
集め、単元HTMLを開かずに再出題する。設問は単元HTMLの `mount()` 引数の
中にあり外から読めないため、`assets/drills.json` に写して配る。

**設問を足す・直す・消したら再生成してコミットする。** 生成物であり
手で編集しない。古いままだとゲート12が落とす。

`mount()` に渡した設定は丸ごと写される。**設定には JSON にできる値だけを
書く。** 関数・`undefined`・`NaN` を書くと生成時に落ちる（黙って消えると、
復習ページでだけ挙動が変わり、画面には何も出ない）。

`review.html` は `assets/drills.json` を `XMLHttpRequest` で読む。単元HTMLと
違い `file://` では動かず、配信が要る（`python3 -m http.server`）。設問の
再出題に単元HTMLを丸ごと読み込む形にすれば `file://` でも動くが、要復習が
複数単元にまたがると100KB超のページを何枚も抱えることになる。

### BokiQuiz の数値検算

**全選択肢が金額を含む設問**には `invariant`（成り立つべき等式）を書く。
書かないとゲート10が落とす。

```js
{
  text: '当座預金の帳簿残高は 540,000円、銀行の残高証明書は 678,000円…',
  choices: ['540,000円', '570,000円', '678,000円', '510,000円'],
  answer: 1,
  invariant: [
    ['540000 + 30000', '678000 - 108000'],   // 企業側 ＝ 銀行側
    ['540000 + 30000', 'answer']             // 正解の選択肢とも一致する
  ]
}
```

`'answer'` は正解の選択肢から取り出した数値。等式は最低1つの数値が
**問題文**に現れていなければならない（選択肢だけで組み立てた式は落とす）。

端数処理などで意図的に一致しない場合は、許容誤差と**その理由**を書く。
`why` のない `tolerance` は落とす。誤差を黙って許すと、本当の計算違いが
そこに隠れる。

```js
['533 * 340 + 533 * 160', '266600',
 { tolerance: 100, why: '単価を丸めた影響で 0.2円 × 500個 ＝ 100円 消える' }]
```

**`invariant` は問題文を検査していない。** 保証しているのは式どうしの
整合であって、問題文の数値との一致ではない。問題文の金額を書き換えて
`invariant` を直し忘れると、その数値が誤答の選択肢として残っている限り
素通りする。問題文を直したら `invariant` も必ず見直すこと。

金額が条件として出てくるだけの設問（「1,000万円以上で2%割戻。この処理は
どれか」）は対象外。答えが金額でないため検算しようがない。

## 学習者個人に合わせた追記

この教材は最終的に一般公開する。同時に、いま使う1人にとっては具体的でなければ
役に立たない。両立させるため、**個人の環境に依存する情報は本文を書き換えず、
専用ブロックとして追記する。**

```html
<div class="callout callout--mine" data-owner="jw-122cl">
  <span class="callout__t">手元の1台（カシオ JW-122CL）の場合</span>
  ...
</div>
```

- 本文は、どの機種・どの環境の読者にも成り立つ一般論のまま保つ。個人の事情で
  一般論を狭めない
- 個人向けの内容は `.callout--mine` に閉じ込める。`data-owner` に対象を書く
- このブロックだけを削除すれば一般公開版になる。逆に言えば、**削除すると本文が
  意味を成さなくなる書き方をしてはいけない**。ブロックは常に「本文の一般論に対する
  自分の場合の答え合わせ」であって、本文の続きではない
- 根拠になる個人の資料（所有機の取扱説明書など）は `reference/` に置き、
  `reference/SOURCES.md` に出典を書く

対象は所有する電卓、受験地、受験回、学習に使える時間帯など。会計の内容そのものは
学習者によって変わらないので、このブロックには入らない。

## 品質ゲート（公開前に必ず通す）

1〜6 と 9〜13 は `npm run check`（機械的検査）、7 と 8 はサブエージェント
（判断を要する検査）。番号は連番だが、実行される場所が違う。

1. **全仕訳の貸借一致**を機械的に検算する（静的な `.jnl` と `BokiJournal` の `debit`/`credit` 両方）
2. **`BokiJournal` の正解科目がすべて `accounts` プールに存在する**か確認する。プールに無いと正解を入力できず、必ず不正解になる
3. `BokiNum` の全解答をスクリプトで再計算する
4. `BokiQuiz` の `answer` インデックスが正解肢を指しているか全問確認する
5. `.toc` のアンカーと `<h2 id>`、`<span class="num">` の番号が一致しているか（節を追加したときの振り直し漏れ）
6. ヘッドレスブラウザでJSエラー・id重複・`data-key`重複・リンク切れを検査する。**横スクロールは 420px 幅でも確認する**（`.grid2` 内の表が典型的な原因）
7. **カバレッジ**：出題区分表と突き合わせ、その単元が扱うべき論点に抜けがないか確認する。到達チェックが「N項目を列挙できる」形なら、そのNが本当に必要十分かを疑う
8. **敵対的検証**：作成時の文脈から切り離し、「誤りがある前提」で会計内容・級の境界・ページ間の矛盾を洗う
9. **進捗記録**：`BokiProgress` が読み込まれ、`mount` 先に `id` があり、実際に1問解いて採点すると記録が増えるか。記録の配線は切れても画面に何も現れないため機械的に見る
10. **`BokiQuiz` の数値検算**：全選択肢が金額を含む設問に `invariant` があり、等式が成り立つか。`tolerance` には `why` を要求する
11. **勘定科目の読み**：`BokiJournal` の `accounts` に出る科目が `assets/app.js` の `YOMI` に登録されているか。読みが無い科目はかなで検索できず漢字でしか引けないが、画面上は何も壊れないため機械的に見る
12. **設問バンクの鮮度**：`assets/drills.json` が単元HTMLと一致しているか。復習ドリルはこれを読んで再出題するため、古いと「教材では直っているのに復習では直っていない」という食い違いが、直した本人には見えない形で残る
13. **記録IDと設問の対応**：`reference/drill-ids.json` と突き合わせ、設問の並びが動いていないか。記録IDの `q` 番号は配列の添字なので、途中に設問を挿すと以降がずれる。ずれても番号としては有効なままで何も壊れて見えないが、学習者の記録は別の設問を指すようになる

### 機械的検査で捕まらないもの

**`BokiQuiz` の数値は、`invariant` を書いた範囲でしか検算されない。**
ゲート10は「全選択肢が金額を含む」設問に `invariant` を要求し、その等式を評価する。
裏を返すと、次は検査されない。

- **金額が条件として出てくるだけの設問。** 対象外なので `invariant` を書く義務もない
- **解説の中の計算。** どのゲートも解説文は評価しない
- **誤答の選択肢の妥当性。** 「別の方法で計算した値」として正しいかは人が見る

問題文の金額を書き換えて `invariant` を直し忘れた場合は落ちる。計算式の
オペランドが問題文に実在するかを見ているため、誤答の選択肢に同じ数値が
残っていても検出される。

**それでも、解説の数値は誰も見ていない。** 問題文を直したら解説も必ず見直す。
ゲートが通ったことを「この設問の数値はすべて正しい」と読み替えないこと。

分類・区分を問うクイズは、**正解が一意になる前提を問題文に書く。**
数値の誤りと違って計算が合わない形では露見せず、学習者が混乱して初めて分かる。

**T字勘定（`.taccounts`）と到達チェックの文言も検査されない。** ゲート1が見るのは
静的な `.jnl` と `BokiJournal` だけで、T字勘定の貸借も、本文の「検算：〜＝〜」という
算式も評価しない。実際に「貸方合計 2,180,000 ＋ 賃率差異 72,000 ＝ 2,180,000」という
恒偽の式と、貸方項目を4つと数える到達チェック（正しくは5つ）が指摘なしで通っていた。
**T字勘定を書いたら貸借を手で合計し、本文の検算式は自分で計算する。**

## 級の境界に関する注意

論点が2級か3級かの判定は、テキストの目次ではなく**出題区分表のPDFから直接確認する**。特に間違えやすいもの：

- **建設仮勘定は2級**（3級ではない）
- **有形固定資産の除却・廃棄、定率法は2級**。3級へ移るのは2027年度改定から（2027/4/1〜）であって、現行の2022年度版では2級
- **伝票会計（3伝票制・仕訳日計表）は3級**。2級テキストには載らないが出題対象
- **消費税（税抜方式）・法人税等・貯蔵品・剰余金の配当と利益準備金の積立は3級**（2019年度改定で3級に追加）。2級テキストに載らないうえ、2級の第3問（決算）・第2問（株主資本等変動計算書）で必修。**Phase 0 の3級総復習で必ず塞ぐ**
- **売上諸掛りの「先方負担＝立替金／売掛金」処理は2021年度改定で3級から削除済み**。現行は送料込みの金額で売上を計上し、支払った送料は発送費（費用）とする。積上げ式なので2級でも旧処理は出ない
- 決算日に原因不明の現金過不足を発見した場合は、現金過不足を経由せず**直接 雑損／雑益**で処理する
- **繰延資産（商-第二-16-0）は1級。** 2級にあるのは「収益と費用」の中の
  創立費・開業費・株式交付費・開発費という**費用の科目だけ**で、勘定科目表にも
  資産の欄の繰延資産科目や「創立費償却」などの償却科目は無い。
  2級の答案で書くのは**支出時の費用処理**であり、資産計上と償却の仕訳は問われない

**科目表に無い科目が要るなら、その論点は級を越えている。** 品質ゲート2を通すために
科目を足すのは、検査を通すために内容を曲げることにあたる。科目が無いことに気づいたら、
まず出題区分表でその論点の級を確認する。

## 制作済み

| フェーズ | 単元 | ファイル |
|---|---|---|
| Phase 0 | 目次・4日間の割り振り・到達チェック | `phase0/index.html` |
| Phase 0 | UNIT 01 3級の総復習（4h）／決算整理9項目・剰余金の配当・建設仮勘定・伝票会計・仕訳ドリル25問 | `phase0/01_3kyu-review.html` |
| Phase 0 | UNIT 02 勘定連絡図（4h）／インタラクティブSVG・穴埋めモード | `phase0/02_kanjo-renrakuzu.html` |
| Phase 0 | UNIT 03 電卓トレーニング（2h）／計算ドリル12問 | `phase0/03_dentaku.html` |
| Phase 0 | UNIT 04 教材調達・受験申込・環境整備（2h） | `phase0/04_junbi.html` |
| Phase 1 | 目次・週ごとの割り振り・到達チェック | `phase1/index.html` |
| Phase 1 W1 | UNIT 01 純資産と資本の関係／銀行勘定調整表（2.5h） | `phase1/01_shou-junshisan-genkin.html` |
| Phase 1 W1 | UNIT 02 商品売買（2.5h）／売上原価対立法・仕入割戻・棚卸減耗・商品評価損 | `phase1/02_shou-shohin.html` |
| Phase 1 W1 | UNIT 03 工業簿記の全体像と原価の分類（3h） | `phase1/03_kou-genka-bunrui.html` |
| Phase 1 W1 | UNIT 04 材料費（3h）／予定価格・材料消費価格差異 | `phase1/04_kou-zairyohi.html` |
| Phase 1 W2 | UNIT 05 収益認識（5h）／履行義務・契約資産と契約負債・按分・変動対価・役務収益 | `phase1/05_shou-shueki-ninshiki.html` |
| Phase 1 W2 | UNIT 06 債務の保証・未決算・研究開発費（2h） | `phase1/06_shou-saiken-saimu.html` |
| Phase 1 W2 | UNIT 07 労務費・経費（4h）／予定賃率・賃率差異・経費の4類型 | `phase1/07_kou-romuhi-keihi.html` |

## 論点の週への割り当て

`論点カバレッジ表.md` に、出題区分表で**2級が扱う論点184件すべて**をPhase 0〜W9のどこで扱うかを
書いてある。単元を新しく作るときは、この表から担当する論点IDを引き、
`meta[name="boki-topics"]` にそのまま入れる。

この表は `reference/syllabus.yml` から起こしたものであり、カリキュラム本文の論点リストからは
起こさない。本文の漏れがそのまま表に移り、漏れたことに気づけなくなる。

**2級の範囲は `grade == 2` だけではない。** grade 3 の論点でも `limit_grade2` を持つものは、
3級の範囲に2級で上乗せがある（商品有高帳は3級だが総平均法は2級、減価償却は3級だが
定率法・生産高比例法は2級、など8件）。上乗せ分は2級で初めて学ぶので、
`grade === 2` だけで抽出すると丸ごと取りこぼす。母数は176件ではなく**184件**。

取りこぼすと「3級で習ったはず」として本文から除外され、どのページでも扱われないまま
カバレッジは緑になる。実際に総平均法がこの経路で抜けていた。

単元を書くときに担当外の論点まで meta に入れると、`npm run coverage` は緑になるのに
実際には薄くしか扱われていない、という状態が起きる。**meta は「このページで学べる」ことの
宣言であって、「言及した」ことの記録ではない。**
